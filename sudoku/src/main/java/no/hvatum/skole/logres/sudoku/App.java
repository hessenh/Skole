package no.hvatum.skole.logres.sudoku;

/**
 * Hello world!
 * 
 */
public class App {
	public static void main(String[] args) {
		// Start grafiks applikasjon
		new GUI().setVisible(true);

		// Om du vil kjøre programmet i konsoll, bruk denne koden:

		/*
		 * SudokuBoard sb; try { sb = new
		 * SudokuBoard("G:/sudoku/sudoku_normal.sudoku");
		 * sb.addDefaultConstraints();
		 * 
		 * sb.printBoard(); long time = System.currentTimeMillis(); sb.solve();
		 * long difference = System.currentTimeMillis() - time; sb.printBoard();
		 * System.out.println("Time used: " + difference + " msec"); } catch
		 * (IOException e) { e.printStackTrace(); }
		 */
	}
}
